<!doctype html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>DataForSEO Enterprise</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <% if (currentUser) { %>
    <nav class="navbar navbar-expand-lg bg-dark navbar-dark mb-4">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">SERP Enterprise</a>
        <div class="navbar-nav me-auto">
          <a class="nav-link" href="/users">Users</a>
          <a class="nav-link" href="/projects">Projects</a>
          <a class="nav-link" href="/tasks">SERP Tasks</a>
          <a class="nav-link" href="/postbacks">Postbacks</a>
        </div>
        <span class="navbar-text me-3"><%= currentUser.name %> (<%= currentUser.role %>)</span>
        <form action="/logout" method="post"><button class="btn btn-outline-light btn-sm">Logout</button></form>
      </div>
    </nav>
    <% } %>
    <main class="container pb-5">
